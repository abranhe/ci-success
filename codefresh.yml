version: '1.0'
steps:
  test_all_the_code:
    image: node:5
    working_directory: ${{main_clone}}
    commands:
      - nvm install 8
      - npm install -g npm@latest
      - npm install
      - npm test
